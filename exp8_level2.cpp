#include<stdio.h>
#include<string.h>
int main()
{
		struct details;
		{
			int mat,phy,che,bio,tam;
		};
		struct detailsobi;
		obi.mat=50;
		obi.phy=55;
		obi.che=58;
		obi.bio=60;
		obi.tam=60;
		printf("mat=%d\n",obi.mat);
		printf("phy=%dn",obi.phy);
		printf("che=%d\n",obi.che);
		printf("boi=%d\n",obi.bio);
		printf("tam=%d\n",obi.tam);
}
